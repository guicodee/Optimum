// import { Link, LinkProps, useLocation } from "react-router-dom";

// export type NavLinkProps = LinkProps;

// export function NavLink(props: NavLinkProps) {
//   const { pathname } = useLocation();

//   return (
//     <Link 
//       data-currenty={pathname === props.to ? 'true' : 'false'}
//       {...props} 
//     />
//   )
// }